import java.util.*; 

class DailyPloy
{ 
	static class Queue 
	{ 
		static Stack<Integer> stack1 = new Stack<Integer>(); 
		static Stack<Integer> stack2 = new Stack<Integer>(); 

		static void enQueue(int x) 
		{ 
			while (!stack1.isEmpty()) 
			{ 
				stack2.push(stack1.pop()); 
			} 
			stack1.push(x);
			while (!stack2.isEmpty()) 
			{ 
				stack1.push(stack2.pop()); 
			} 
		}     
      	static int deQueue() 
		{ 
			if (stack1.isEmpty()) 
			{ 
				System.out.println("Queue is Empty"); 
			} 
			int p = stack1.peek(); 
			stack1.pop(); 
			return p; 
		}	 
	}; 
  
	public static void main(String[] args) 
	{ 
		Queue q = new Queue();
		Scanner input = new Scanner (System.in);
		System.out.println("Enter the number of elements you want in queue :"); 
		int n = input.nextInt();
		for(int i=0;i<n;i++) q.enQueue(input.nextInt()); 
        for(int i=0;i<n;i++) System.out.println(q.deQueue()); 
	} 
} 
